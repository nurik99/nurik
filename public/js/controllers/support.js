app.controller('SupportCtrl', SupportCtrl);

SupportCtrl.$inject = ['$http', '$scope', 'basket', '$interval','$state', '$rootScope', '$cookies'];

function SupportCtrl($http, $scope, basket, $interval, $state, $rootScope, $cookies) {
	var vm = this;
}